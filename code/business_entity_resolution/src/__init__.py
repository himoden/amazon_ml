"""Amazon ML Challenge 2026 - Business Entity Resolution.

Configurable pipeline package. All heavy (third-party) imports are lazy
(loaded inside functions) so this package can be imported cheaply and the
modules can be syntax/lint-checked in any environment.
"""

__version__ = "0.1.0"